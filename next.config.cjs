/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // transpilePackages: ['packages','@chadamour/header'],
  transpilePackages: ['@chadamour/header', '@chadamour/layout'],
};

export default nextConfig;
